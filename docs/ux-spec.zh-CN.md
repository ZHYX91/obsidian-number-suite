---
doc_id: ux-spec
language: zh-CN
source_language: zh-CN
translation_status: source
status: stable
last_synced: 2026-08-23
---

# UX 规格

[English synced translation](ux-spec.en.md)

<!-- section: authority -->
## 文档权威性

本文是交互与文案行为的中文规范源；英文文件为同步译本。视觉截图和历史验收记录只能证明
对应版本，不覆盖本文。

<!-- section: principles -->
## 交互原则

- 始终区分源文件状态和 Obsidian 中的视觉状态。
- 任何文件修改必须先让用户看到逐项结果和风险。
- 默认选项应保守，歧义情况应保留原文并给出原因。
- 全局值、当前笔记覆盖值和最终生效值必须可区分。
- 移动端、窄窗口、键盘和触摸操作必须保持可达，不依赖悬停。

<!-- section: entry-points -->
## 入口

功能区图标和命令面板打开当前笔记控制面板。命令面板还提供恢复源样式、独立切换虚拟
显示/隐藏、当前笔记写入/清理/重新编号、移除来源标记、批处理和撤销最近批处理。没有活动
Markdown 笔记时，当前笔记操作不可执行并显示明确提示。

<!-- section: current-note-panel -->
## 当前笔记控制面板

面板先显示笔记名称和路径，再用“设置项 / 全局值 / 当前笔记覆盖 / 最终生效值”摘要呈现
虚拟显示、实体隐藏、方案和完全忽略。显示与隐藏均使用“跟随全局 / 开启 / 关闭”三态。
方案下拉只展示可用名称，不要求用户输入内部 ID。

“跟随全局”删除对应 Property；“全部恢复”删除所有插件覆盖并保留其他 Properties。成功写入
后面板必须退出忙碌状态、重新读取 Properties，并立即刷新实时预览和阅读视图。读取失败时
禁止盲写并提供重试。

<!-- section: settings-experience -->
## 全局设置体验

设置统一分为七个选项卡：常规、标题编号、题注、交叉引用、注释、写入与清理、显示与批处理。内置方案可
展开查看并复制为自定义方案。自定义方案必须
提供名称、起始层级、H1-H6 模板实时预览和精确排除列表。空模板显示“该层级不编号”；非空
模板语义错误时，保存按钮不得提交，并显示可理解的约束说明。

频繁设置修改可以合并保存，但界面必须显示保存中、失败原因和重试入口。不能把未持久化值
显示成已保存。

<!-- section: previews-and-warnings -->
## 预览与警告

当前笔记和批处理预览必须列出文件、行号、修改前后文本、识别来源及警告。预览过期时整体
取消。歧义前缀、多重前缀、空标题、缺少父层或不支持的前缀必须解释为何未修改。实体操作
必须提醒标题链接风险。

<!-- section: display-behavior -->
## 显示行为

虚拟序号在视觉上位于标题文本之前，但不得进入复制文本或可编辑源范围。隐藏实体序号时，
光标或选择触及标题行应恢复实体文本，IME composition 期间应撤销装饰。阅读视图在源码标题
与渲染标题数量或层级不一致时不做部分猜测。

识别到的题注在视觉上从 `Figure:` 变为 `Figure 1:`（Table、Equation、Code 同理），但不
修改源码。显式语义引用只把前导 `@` 视觉替换为已解析编号和一个空格，保留原生 Obsidian
链接文本、别名、点击行为和目标。目标缺失、重复、跨文件或没有可见有效编号时，完整源码
渲染保持原样。IME composition 期间撤销全部语义装饰，插件清理时恢复所有被替换的 `@`。

脚注与尾注在每个文件内分别显示按首次引用顺序计算的序号；重复引用复用首次序号。装饰只
替换可见引用/定义标签，保留 Obsidian 原生链接、列表结构和源码。定义缺失、重复、规范化
冲突或渲染节点数量不匹配时保持原样；重新处理或插件清理时恢复原有标签和列表值。

<!-- section: accessibility-and-mobile -->
## 无障碍与移动端

虚拟装饰应从辅助技术语义中隐藏，实体内容仍可访问。控件必须有文字名称、键盘焦点和合理
触摸尺寸；摘要在窄屏可重排而不丢字段。移动端支持声明只基于对应版本的带日期记录，模拟器
结果不得表述为实体设备通过。

<!-- section: error-recovery -->
## 错误与恢复

设置保存失败保留待保存快照。批处理失败只回滚仍精确匹配插件写入结果的文件，并保留可用
恢复记录。并发编辑、损坏恢复数据或宿主渲染差异都应失败关闭，不能以成功提示掩盖未完成
工作。
